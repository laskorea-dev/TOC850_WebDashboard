# 📘 TOC B2B 멀티테넌시 계측 모니터링 시스템 최종 완료보고서 & 사용자 매뉴얼

본 문서는 다중 계측기 동기화 및 B2B 멀티테넌시(고객사별 독립 데이터 격리) 대시보드 시스템의 운용, 환경 설정 및 라이브 배포를 위한 종합 사용자 가이드북입니다.

---

## 1. 1차 개발 성과 (Google Sheets 연동 프로토타입)

초기 프로토타입은 서버나 클라우드 비용 없이 구글 스프레드시트를 클라우드 DB 대용으로 사용하는 방식으로 구축되었습니다.

1. **구글 시트 기반 물리 보안 격리 (0원 무서버 B2B)**:
   * 고객사마다 별도의 구글 스프레드시트를 개설하고 웹에 게시(.csv)하여 대시보드와 연동하였습니다.
2. **다중 기기 식별자 (`Device_ID`) 탑재**:
   * 동일한 시트 안에서 여러 대의 계측기를 가동할 수 있도록 데이터 행마다 `Device_ID`를 매립 전송하였습니다.
3. **Tkinter GUI 업로더 개발**:
   * 로컬 SQLite DB 경로, 시트 이름, 가동 주기 등을 윈도우 창에서 입력받아 구글 API로 밀어주는 인터페이스를 적용하였습니다.

*(세부 설정 방법은 구 버전 가이드라인을 참조하십시오.)*

---

## 2. 2차 개발 성과: Supabase 전환 및 알림 고도화 (2026년 6월 18일)

더 나은 실시간성 확보와 데이터 관리의 체계화, 그리고 관리 효율성 증대를 위해 **구글 시트 방식에서 Supabase(PostgreSQL Cloud) 데이터베이스 구조로 마이그레이션**하고, **경고 임계값 설정 및 메일 발송 자동화** 기능을 새롭게 통합하였습니다.

### A. 주요 개선 내역

1. **데이터 저장소 마이그레이션 (Supabase 전환)**:
   * 실시간성이 뛰어난 Supabase를 사용하여 데이터 적재 및 조회를 처리함으로써 속도 및 데이터 트랜잭션의 안정성을 극대화하였습니다.
2. **트렌드 차트 표시범위 최적화 (24시간)**:
   * 대시보드 접속 시 초기 데이터 표시범위를 기존 7일(`7d`)에서 **24시간(`24h`)**으로 축소하여 첫 로딩 속도를 향상시켰습니다.
3. **경고 알림 시각화 및 DB 연동**:
   * 계측 수치 상태를 **평시(흰색) · 경고(빨강)**로 구분하여 대시보드 표와 경보 램프로 시각화하였습니다. (오알람 방지를 위해 기존의 '주의' 단계는 폐기)
   * 채널별 경고 임계치와 알람 수신자 이메일 목록을 웹 대시보드에서 직접 수정할 수 있게 하였으며, 기존 DB 스키마 DDL 수정 없이 `toc_alert_high` 컬럼의 JSON 데이터 내에 포장하여 Supabase에 동기화합니다.
   * **임계치 유효성 검사 완화**: 설정 저장 시 5000 미만 임계값에 대해 뜨던 경고 확인창(confirm)을 완전히 제거하여, 50 ppm (방류수), 1000 ppm (고농도조) 등 5000 미만의 커스텀 값을 입력하더라도 경고 팝업 없이 즉시 원클릭으로 저장되도록 개선하였습니다. (0 미만의 음수가 입력되는 경우에만 정상 범위 지정을 위한 검증 메시지가 노출됩니다.)
   * 임계치 지정이 없는 경우의 기본 폴백 기준치는 다음과 같습니다:
     * **Ch 3 (방류수)**: 경고 50 (ppm)
     * **Ch 2 (고농도조)**: 경고 1000 (ppm)
     * **Ch 1 (원수조)**: 경고 2000 (ppm)
4. **로컬 SMTP 경고 메일 발송 자동화**:
   * 계측기 PC의 파이썬 업로더(`gui_uploader.py`)가 수집한 신규 데이터 중 경고 등급에 해당하는 건이 발생하면 로컬 SMTP 서버(`wsmtp.ecounterp.com`)를 경유해 수신자들에게 실시간 경보 메일을 보냅니다.
   * 메일 폭주를 막기 위해 채널별 **1시간(3600초)의 메일 발송 쿨다운 타이머**가 적용됩니다.
5. **독립 관리자 테스트 페이지 (`?admin`) 및 데이터 격리**:
   * 일반 사용자용 모니터링 화면을 방해하지 않고 알람 기능을 테스트할 수 있도록 `?admin` 주소 분기 샌드박스를 구축하였습니다.
   * **보안 통제**: 대시보드 전용 패스코드(`850`) 검증을 거친 사용자만 테스트 모드에 진입할 수 있습니다.
   * **테스트 데이터 격리**: 테스트용 모의 데이터(`Add_note` 컬럼이 `[MOCK TEST DATA]`인 행)는 메인 사용자 화면 및 차트에서 자동 제외 처리됩니다.
   * **비동기 이메일 테스트**: 웹에서 발송을 요청하면 Supabase의 `trigger_test_email: true` 플래그를 수정하고, 로컬 파이썬 uploader가 이를 감지하여 실제 발송 후 리셋하는 비동기 메커니즘을 적용했습니다.
6. **알람 인터페이스 추상화 및 팩토리 패턴 도입**:
   * 향후 카카오톡이나 MMS 알람 모듈 도입을 용이하게 하기 위해 기존 하드코딩된 이메일 발송 기능을 `alerts/` 패키지로 완전히 모듈화 및 격리하였습니다.
   * `BaseAlertSender` 추상 인터페이스를 상속받은 `EmailAlertSender` 클래스를 설계하였으며, 생성 팩토리 함수인 `get_alert_sender`를 제공합니다.
   * `uploader_config.json` 내의 `"alert_type"` 설정을 통해 메인 업로더 코드 수정 없이 알람 발송 모듈을 통째로 교체 가능한 구조를 마련하였습니다.

---

## 3. 3차 개발 성과: 단일 테이블 통합, 텔레그램 연동 및 Zero-Touch 자동 등록 (2026년 7월 7일)

데이터베이스의 효율성 증대와 비용 없는 실시간 푸시 알람 도입, 그리고 장비 추가 시 관리자 설정의 편의성을 극대화하기 위해 **단일 테이블 통합(Single-Table) 설계**, **텔레그램 알람 연동**, **Zero-Touch 장비 자동 등록**을 완료하였습니다.

1. **데이터베이스 단일 테이블 통합 (Single-Table)**:
   * 장비 추가 시마다 테이블을 개별 생성해야 했던 불필요함을 해소하기 위해 모든 장비가 `measure_logs` 단일 테이블을 공유하도록 아키텍처를 개선했습니다.
   * 웹 대시보드에서 빌드타임 환경변수에 의존하지 않고, DB 설정값(`use_single_table: true`)에 따라 기존 개별 테이블 모드와 공용 테이블 모드를 실시간 동적 자동 전환하도록 개선하여 1개의 웹 URL로 기존/신규 고객을 완전 동시 수용합니다.
   * 업로더 및 웹 대시보드에서 `Device_ID` 기반의 조건 필터링(`&Device_ID=eq.장비ID`)을 반드시 수행하도록 하여, 동일 테이블을 활용하면서도 논리적으로 기기 데이터를 격리 조회할 수 있도록 리팩토링했습니다.
2. **텔레그램 무료 다대다 푸시 알림 모듈 구축**:
   * 텔레그램 봇 API를 활용해 별도의 비용 없이 실시간 경고를 모바일로 수신할 수 있는 `TelegramAlertSender` 모듈을 신규 제작하였습니다 (`alerts/telegram_sender.py`).
   * 쉼표(,)로 구분된 여러 명의 Chat ID 혹은 단톡방/채널 ID로 알림 메시지를 개별 전송합니다.
   * 이메일용 HTML 본문을 텔레그램이 지원하는 안전한 HTML 태그(<b>, <i>, <a> 등)만 골라내고 포맷팅해 주는 HTML-to-Telegram Formatter 헬퍼가 내장되어 텍스트 깨짐 없이 깔끔한 텔레그램 뷰를 보장합니다.
3. **Zero-Touch 장비 자동 등록 (Auto-Registration)**:
   * 업로더 프로그램 기동 시 Supabase 설정 테이블(`850_dashboard_site_config`)을 조회하여 해당 장비 ID 설정 행이 없으면, 업로더가 직접 기본 설정값을 입력하여 새 행을 자동 등록(POST)하는 메커니즘을 추가했습니다.
   * 이로 인해 웹에서 관리자가 수동으로 DB에 행을 추가하는 오버헤드를 완전 차단하고 무설정 플러그앤플레이(Plug-and-Play) 배포가 가능해졌습니다.
4. **PC 하드웨어 기반 고유 ID 자동 생성 (Auto Device ID)**:
   * 로컬 설정 파일(`uploader_config.json`)의 `device_id` 속성이 비어있거나 `"auto"`인 경우, 현장 PC의 고유한 MAC 주소를 긁어와 중복되지 않는 하드웨어 기기 ID(예: `DEV_3C060A2B1D3E`)를 자동 빌드해 적용하도록 구현했습니다.
5. **기존 DB 스키마 무수정 설계 규칙 준수**:
   * 추가적인 테이블이나 컬럼 생성 없이, `850_dashboard_site_config` 테이블의 기존 `toc_alert_high` jsonb 데이터에 `telegram_chat_ids` 및 모의 테스트 플래그 `trigger_test_telegram`을 매립하여 안전하게 저장 및 동기화합니다.
6. **웹 대시보드 설정 UI 확장**:
   * 대시보드 설정 모달(일반 모드 및 `?admin` 모드)에 "알림 수신 텔레그램 Chat ID" 입력 필드를 추가하였습니다.
   * "테스트 텔레그램 발송" 버튼을 신설하여, 클릭 시 비동기로 업로더가 신호를 감지해 즉시 텔레그램 메시지를 발송하는 인터랙티브 테스트 시나리오를 구성했습니다.

---

## 4. 4차 개발 성과: V2 스키마 설계 및 지점 식별자 직접 매립 아키텍처 (2026년 7월 7일)

물리적인 기기 ID(`Device_ID`)에만 쿼리를 의존하여 발생하던 PC 교체 시 데이터 단절 문제를 해결하기 위해, 최종 납품 지점을 기준으로 묶을 수 있는 **지점 식별자(Site_ID) 직접 매립 아키텍처 및 Supabase V2 데이터 모델**을 수립하였습니다.

1. **지점 설정(`site_config_v2`) 및 계측 로그(`measure_logs_v2`) 신규 스키마**:
   * 관계형 설계에 최적화되고 쿼리가 단순화된 신규 V2 테이블 스키마를 수립하고, 사용자가 SQL Editor에서 한 번에 테이블을 생성할 수 있도록 SQL DDL을 배포 가이드에 수록했습니다.
2. **지점(Site_ID)과 장치(Device_ID)의 역할 분리**:
   * `uploader_config.json` 설정을 통해 각 PC가 장비명(`device_id`)과 지점명(`site_id`)을 개별 탑재할 수 있도록 업로더 코드를 보완하였습니다.
   * 계측 로그 데이터 적재 시 두 컬럼이 함께 포함되어 기록되므로, 물리 기기 교체 시에도 이전 기기 데이터와 신규 기기 데이터를 지점 코드 하나로 묶어 무중단 모니터링이 가능해졌습니다.
3. **업로더 v5.0 컴파일 및 배포 문서 제공**:
   * `gui_uploader_v5.0.exe` 파일 컴파일을 완료하고, 타사 배포 및 세팅 가이드를 친절하게 수록한 `gui_uploader_v5.0_README.md`를 루트 폴더에 작성하였습니다.

---

## 5. 5차 개발 성과: 고객 알림 자가 등록 및 관리자 삭제 권한 분리 (2026년 7월 7일)

고객 담당자가 직접 연락처를 등록하여 모니터링 경보를 받도록 자가 등록 양식을 신설하고, 관리자가 이 수신처들의 이름과 연락처 매핑 정보를 인지한 상태에서 안전하게 삭제/제어하도록 권한 분리형 알람 관리 체계를 구축했습니다.

1. **고객 전용 알림 자가 등록 양식 (Public Registration Form)**:
   * 대시보드 화면 헤더 영역에 비밀번호 없이 접근 가능한 `🔔 알림 수신 등록` 버튼을 추가했습니다.
   * 이름(부서/직급 포함), 알림 매체(이메일/텔레그램), 수신처 값을 기입하고 저장하면 `site_config_v2` 테이블의 JSONB 구조(`receivers` 배열)에 자동으로 데이터가 매핑되어 쌓입니다.
2. **관리자 전용 알림 수신처 삭제 및 제어 (Admin-Only Control)**:
   * 패스코드 `850` 인증을 통과한 뒤 진입하는 설정 모달 내부 하단에 등록된 수신처 매핑 테이블을 렌더링하도록 했습니다.
   * 단순 접속 고객은 이 테이블을 조회만 할 수 있고, `?admin` URL 매개변수로 진입한 관리자(Admin)에게만 `🗑️ 삭제` 버튼이 작동하도록 비즈니스 인가 장치를 통합했습니다.
3. **하위 호환 콤마 변환 동기화**:
   * 웹 대시보드에서 신규 사용자를 등록하거나 기존 수신처를 삭제할 때마다, 구조화된 `receivers` JSON 배열을 자동으로 파싱하여 콤마로 나열된 문자열로 병합합니다.
   * 이 문자열을 DB의 `alert_emails` 및 `telegram_chat_ids` 컬럼에 실시간으로 동시에 PATCH 함으로써 현장 PC 업로더 프로그램의 코드를 전혀 변경하지 않고도 완벽하게 알람 발송 처리가 이루어지도록 호환성을 만족시켰습니다.

---

## 6. 6차 개발 성과: 업로더 프로그램 UI 현대화 및 지점 한글명 설정 폼 연동 (2026년 7월 7일)

현장 PC용 업로더 GUI의 UI/UX를 프리미엄 다크 네이비 테마로 리뉴얼하고, 관리자가 대시보드 상에서 지점 한글명을 직접 제어할 수 있는 편의 기능을 추가했습니다.

1. **업로더 GUI 리뉴얼 (v5.1)**:
   * **프리미엄 테마**: Segoe UI(영문) 및 맑은 고딕(한글) 폰트를 도입하고, B2B 웹 대시보드와 통일된 다크 네이비/시안 테마(`#0B0F19`)를 구축했습니다.
   * **LED 인디케이터**: 원형 캔버스 기반 LED 상태등(가동 시 초록색, 일시정지 시 주황색)을 적용하여 기기 작동 유무를 시각화했습니다.
   * **폴더블(접이식) 레이아웃**: 기본 설정(SQLite DB 경로, Site ID)만 노출되도록 창 크기를 `640x545`로 고정하고, 복잡한 인프라 설정은 `➕ 상세 인프라 설정 표시`를 클릭할 때만 창 크기가 `640x785`로 확장되며 나타나도록 이원화했습니다.
   * **원클릭 일괄 설정 저장**: 개별 필드마다 존재하던 저장 단추들을 단 하나의 `💾 모든 설정 및 자격 증명 저장` 버튼으로 통합하여 설정값을 `uploader_config.json`에 영구히 저장할 수 있도록 편의성을 개선했습니다.
2. **웹 대시보드 지점 한글명 제어**:
   * 대시보드 설정 모달(`⚙ 설정` - 패스코드 gated) 최상단에 "🏢 지점 한글명 설정" 필드를 추가하여, 수정 시 `site_config_v2` 테이블의 `site_name`이 갱신되어 상단 헤더에 동적 연동되도록 조치했습니다.
3. **배포 패키지 v5.1 리비전**:
   * `계측기_PC_배포패키지_v5.1` 폴더 내에 Supabase URL, Key, default Telegram token을 기본값으로 기입해 두어 복사 후 즉시 기동이 가능하도록 준비했습니다.

---

## 7. 7차 개발 성과: 업로더 설정 간소화 및 세부 주기/기동 제어 고도화 (2026년 7월 7일)

1. **지점 식별자(Site ID) 입력 제거를 통한 단순화**:
   * 현장 작업자의 조작 실수를 원천 방지하기 위해 불필요했던 Site ID 입력 필드를 GUI 설정 카드에서 제거했습니다. 내부적으로는 config 파일의 `site_id` 값을 안전하게 조회 및 영구 저장 유지합니다.
2. **사이트 이름 라벨 직관화**:
   * 개발자 관점의 설명이었던 "사이트 이름 (지점 한글명 - site_name)" 문구를 깔끔하고 직관적인 "사이트 이름 (Site Name)"으로 수정했습니다.
3. **분 단위 자동 동기화 주기 설정 신설**:
   * 사용자가 원하는 주기를 분 단위로 직접 타이핑하여 입력할 수 있도록 인풋 필드를 배치하고, 저장 시 백그라운드 초 단위 (`interval_seconds`) 설정값으로 변환하여 config 저장 및 타이머 카운트다운 엔진에 실시간 적용되도록 구현했습니다.
4. **기동 시 자동 동기화 활성 상태 지정**:
   * "시작 시 자동 동기화 활성화" 체크박스를 새롭게 추가하여, 현장 PC 부팅 및 uploader 프로그램 가동 시 데이터 동기화를 즉시 진행할지 일시정지 상태 (`is_paused = True`)로 로드할지 GUI에서 체크 한 번으로 지정하고 `is_paused` 설정을 영구 동기화하도록 처리했습니다.
5. **실시간 원격 동적 새로고침 연동**:
   * 웹 대시보드에서 지점 한글명을 갱신하면 현장 업로더가 스레드를 통해 감지하는 즉시 UI 라벨과 로컬 config 설정 파일을 동적으로 자동 업데이트하도록 상호 동기화 로직을 마무리했습니다.

---

## 8. 최종 품질 및 동작 검증 결과
* **2026-06-18 배포 및 검증 완료**: 로컬 빌드 컴파일 확인 및 SMTP 연동 확인 완료. 깃허브 원격 푸시 후 Vercel 라이브 환경에서 번들 컴포넌트 네임스페이스 충돌 및 컴포넌트 초기화 전 TDZ 참조 오류(`ReferenceError: Cannot access 'loadData' before initialization`)로 발생했던 화면 멈춤(White Screen) 크래시 현상을 해결하기 위해, `LegacyApp` 명칭 고유화 및 `loadData` 함수의 선언 순서를 끌어올려 핫픽스 배포를 최종 완료하고 정상 작동을 검증했습니다.
* **2026-07-07 3차 개발 및 빌드 검증 완료**:
  * 파이썬 파일(`gui_uploader.py`, `alerts/telegram_sender.py`, `alerts/__init__.py`) 구문 컴파일 검증 성공.
  * 웹 대시보드 리팩토링 후 Vite 프로덕션 번들 빌드(`npm run build`) 오류 없이 빌드 완료 확인.
  * 신규 장비의 무설정 기동 및 동적 DB 판별 로직 추가 검증 완료.
* **2026-07-07 4차 V2 설계 및 데이터 연속성 연동 완료**:
  * `site_config_v2` 및 `measure_logs_v2` 기반 웹 대시보드 데이터 조회/수정/삭제 쿼리 정합성 검증 완료.
  * 파이썬 GUI 업로더 v5.0 빌드 및 copy, README 가이드 및 배치파일 호환성 업데이트 완료.
* **2026-07-07 5차 고객 자가등록 및 관리자 제어 권한 분리 완료**:
  * `App.jsx` 내 `registerReceiver` 및 `deleteReceiver` 비즈니스 로직 작성 및 컴파일 테스트 성공.
  * Vercel 프로덕션 신규 배포를 통한 웹 라이브 갱신 완료 (`https://dashboard-ochre-two-35.vercel.app`에 즉시 반영).
  * 자가 등록 완료 시 Supabase `site_config_v2` 내의 JSONB(`receivers`) 및 flat columns(`alert_emails`, `telegram_chat_ids`)가 실시간 동기 업데이트됨을 검증했습니다.
* **2026-07-07 6차 업로더 v5.1 UI 현대화 및 지점 한글명 제어 완료**:
  * 현장 PC uploader GUI 화면을 프리미엄 다크 네이비 테마로 리뉴얼하고, 접이식 상세 설정 윈도우 및 영구 저장 기능을 완성했습니다.
  * 대시보드 어드민 설정 폼에서 지점 한글명을 기입하고 저장 시, Supabase의 `site_name`이 즉시 갱신되어 상단 헤더에 동적 연동 표시됨을 확인했습니다.
  * 배포 패키지 v5.1 내에 SUPABASE URL/Key 및 텔레그램 봇 토큰 값을 매립 동봉하여 현장 납품 및 구동 준비 상태를 완료했습니다.
* **2026-07-07 7차 업로더 설정 간소화 및 세부 주기/기동 제어 완료**:
  * `gui_uploader.py` 내의 Site ID 필드 제거, 사이트 이름 레이블 단순화, 분 단위 주기 입력 및 기동 체크박스 위젯의 GUI 마운트 및 크래시 없는 컴파일 작동 확인.
  * 현장 PC 배포 패키지 v5.1 디렉토리 내에 반영 완료 및 uploader_config.json에 기입 검증 완료.

