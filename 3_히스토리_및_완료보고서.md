# 📘 TOC B2B 멀티테넌시 계측 모니터링 시스템 최종 완료보고서 & 사용자 매뉴얼

본 문서는 다중 계측기 동기화 및 B2B 멀티테넌시(고객사별 독립 데이터 격리) 대시보드 시스템의 운용, 환경 설정 및 라이브 배포를 위한 종합 사용자 가이드북입니다.

---

## 1. 1차 개발 성과 (Google Sheets 연동 프로토타입)

초기 프로토타입은 서버나 클라우드 비용 없이 구글 스프레드시트를 클라우드 DB 대용으로 사용하는 방식으로 구축되었습니다.

1. **구글 시트 기반 물리 보안 격리 (0원 무서버 B2B)**:
   * 고객사마다 별도의 구글 스프레드시트를 개설하고 웹에 게시(.csv)하여 대시보드와 연동하였습니다.
2. **다중 기기 식별자 (`Device_ID`) 탑재**:
   * 동일한 시트 안에서 여러 대의 계측기를 가동할 수 있도록 데이터 행마다 `Device_ID`를 매립 전송하였습니다.
3. **Tkinter GUI 업로더 개발**:
   * 로컬 SQLite DB 경로, 시트 이름, 가동 주기 등을 윈도우 창에서 입력받아 구글 API로 밀어주는 인터페이스를 적용하였습니다.

*(세부 설정 방법은 구 버전 가이드라인을 참조하십시오.)*

---

## 2. 2차 개발 성과: Supabase 전환 및 알림 고도화 (2026년 6월 18일)

더 나은 실시간성 확보와 데이터 관리의 체계화, 그리고 관리 효율성 증대를 위해 **구글 시트 방식에서 Supabase(PostgreSQL Cloud) 데이터베이스 구조로 마이그레이션**하고, **경고 임계값 설정 및 메일 발송 자동화** 기능을 새롭게 통합하였습니다.

### A. 주요 개선 내역

1. **데이터 저장소 마이그레이션 (Supabase 전환)**:
   * 실시간성이 뛰어난 Supabase를 사용하여 데이터 적재 및 조회를 처리함으로써 속도 및 데이터 트랜잭션의 안정성을 극대화하였습니다.
2. **트렌드 차트 표시범위 최적화 (24시간)**:
   * 대시보드 접속 시 초기 데이터 표시범위를 기존 7일(`7d`)에서 **24시간(`24h`)**으로 축소하여 첫 로딩 속도를 향상시켰습니다.
3. **경고 알림 시각화 및 DB 연동**:
   * 계측 수치 상태를 **평시(흰색) · 경고(빨강)**로 구분하여 대시보드 표와 경보 램프로 시각화하였습니다. (오알람 방지를 위해 기존의 '주의' 단계는 폐기)
   * 채널별 경고 임계치와 알람 수신자 이메일 목록을 웹 대시보드에서 직접 수정할 수 있게 하였으며, 기존 DB 스키마 DDL 수정 없이 `toc_alert_high` 컬럼의 JSON 데이터 내에 포장하여 Supabase에 동기화합니다.
   * **임계치 유효성 검사 완화**: 설정 저장 시 5000 미만 임계값에 대해 뜨던 경고 확인창(confirm)을 완전히 제거하여, 50 ppm (방류수), 1000 ppm (고농도조) 등 5000 미만의 커스텀 값을 입력하더라도 경고 팝업 없이 즉시 원클릭으로 저장되도록 개선하였습니다. (0 미만의 음수가 입력되는 경우에만 정상 범위 지정을 위한 검증 메시지가 노출됩니다.)
   * 임계치 지정이 없는 경우의 기본 폴백 기준치는 다음과 같습니다:
     * **Ch 3 (방류수)**: 경고 50 (ppm)
     * **Ch 2 (고농도조)**: 경고 1000 (ppm)
     * **Ch 1 (원수조)**: 경고 2000 (ppm)
4. **로컬 SMTP 경고 메일 발송 자동화**:
   * 계측기 PC의 파이썬 업로더(`gui_uploader.py`)가 수집한 신규 데이터 중 경고 등급에 해당하는 건이 발생하면 로컬 SMTP 서버(`wsmtp.ecounterp.com`)를 경유해 수신자들에게 실시간 경보 메일을 보냅니다.
   * 메일 폭주를 막기 위해 채널별 **1시간(3600초)의 메일 발송 쿨다운 타이머**가 적용됩니다.
5. **독립 관리자 테스트 페이지 (`?admin`) 및 데이터 격리**:
   * 일반 사용자용 모니터링 화면을 방해하지 않고 알람 기능을 테스트할 수 있도록 `?admin` 주소 분기 샌드박스를 구축하였습니다.
   * **보안 통제**: 대시보드 전용 패스코드(`850`) 검증을 거친 사용자만 테스트 모드에 진입할 수 있습니다.
   * **테스트 데이터 격리**: 테스트용 모의 데이터(`Add_note` 컬럼이 `[MOCK TEST DATA]`인 행)는 메인 사용자 화면 및 차트에서 자동 제외 처리됩니다.
   * **비동기 이메일 테스트**: 웹에서 발송을 요청하면 Supabase의 `trigger_test_email: true` 플래그를 수정하고, 로컬 파이썬 uploader가 이를 감지하여 실제 발송 후 리셋하는 비동기 메커니즘을 적용했습니다.
6. **알람 인터페이스 추상화 및 팩토리 패턴 도입**:
   * 향후 카카오톡이나 MMS 알람 모듈 도입을 용이하게 하기 위해 기존 하드코딩된 이메일 발송 기능을 `alerts/` 패키지로 완전히 모듈화 및 격리하였습니다.
   * `BaseAlertSender` 추상 인터페이스를 상속받은 `EmailAlertSender` 클래스를 설계하였으며, 생성 팩토리 함수인 `get_alert_sender`를 제공합니다.
   * `uploader_config.json` 내의 `"alert_type"` 설정을 통해 메인 업로더 코드 수정 없이 알람 발송 모듈을 통째로 교체 가능한 구조를 마련하였습니다.

---

## 3. 3차 개발 성과: 단일 테이블 통합, 텔레그램 연동 및 Zero-Touch 자동 등록 (2026년 7월 7일)

데이터베이스의 효율성 증대와 비용 없는 실시간 푸시 알람 도입, 그리고 장비 추가 시 관리자 설정의 편의성을 극대화하기 위해 **단일 테이블 통합(Single-Table) 설계**, **텔레그램 알람 연동**, **Zero-Touch 장비 자동 등록**을 완료하였습니다.

1. **데이터베이스 단일 테이블 통합 (Single-Table)**:
   * 장비 추가 시마다 테이블을 개별 생성해야 했던 불필요함을 해소하기 위해 모든 장비가 `measure_logs_v2` 단일 테이블을 공유하도록 아키텍처를 개선했습니다.
   * 웹 대시보드에서 빌드타임 환경변수에 의존하지 않고, DB 설정값(`use_single_table: true`)에 따라 기존 개별 테이블 모드와 공용 테이블 모드를 실시간 동적 자동 전환하도록 개선하여 1개의 웹 URL로 기존/신규 고객을 완전 동시 수용합니다.
   * 업로더 및 웹 대시보드에서 `Device_ID` 기반의 조건 필터링을 반드시 수행하도록 하여, 동일 테이블을 활용하면서도 논리적으로 기기 데이터를 격리 조회할 수 있도록 리팩토링했습니다.
2. **텔레그램 무료 다대다 푸시 알림 모듈 구축**:
   * 텔레그램 봇 API를 활용해 별도의 비용 없이 실시간 경고를 모바일로 수신할 수 있는 `TelegramAlertSender` 모듈을 신규 제작하였습니다 (`alerts/telegram_sender.py`).
   * 쉼표(,)로 구분된 여러 명의 Chat ID 혹은 단톡방/채널 ID로 알림 메시지를 개별 전송합니다.
   * 이메일용 HTML 본문을 텔레그램이 지원하는 안전한 HTML 태그(<b>, <i>, <a> 등)만 골라내고 포맷팅해 주는 HTML-to-Telegram Formatter 헬퍼가 내장되어 텍스트 깨짐 없이 깔끔한 텔레그램 뷰를 보장합니다.
3. **Zero-Touch 장비 자동 등록 (Auto-Registration)**:
   * 업로더 프로그램 기동 시 Supabase 설정 테이블을 조회하여 해당 장비 ID 설정 행이 없으면, 업로더가 직접 기본 설정값을 입력하여 새 행을 자동 등록(POST)하는 메커니즘을 추가했습니다.
4. **PC 하드웨어 기반 고유 ID 자동 생성 (Auto Device ID)**:
   * 로컬 설정 파일(`uploader_config.json`)의 `device_id` 속성이 비어있거나 `"auto"`인 경우, 네트워크 어댑터 MAC 주소를 해싱한 고유 식별코드로 자동 대체 주입되도록 설계하여 설정 수작업을 배제했습니다.

---

## 4. 4차 개발 성과: 다중 기기 독립 데이터 적재 아키텍처 수립 (2026년 7월 7일)

동일 지점에 여러 개의 PC 및 계측 장비를 배치하는 경우를 위해 DB 덮어쓰기 현상을 예방하고, 안전하게 데이터를 분할 쿼리할 수 있도록 장비명(`device_id`)과 지점명(`site_id`)을 데이터 적재 페이로드에 고유 탑재하였습니다.

---

## 5. 5차 개발 성과: 고객 알림 자가 등록 및 관리자 삭제 권한 분리 (2026년 7월 7일)

고객 담당자가 직접 연락처를 등록하여 모니터링 경보를 받도록 자가 등록 양식을 신설하고, 관리자가 이 수신처들의 등록 정보를 인지한 상태에서 안전하게 삭제/제어하도록 권한 분리형 알람 관리 체계를 구축했습니다.

1. **고객 전용 알림 자가 등록 양식**:
   * 대시보드 화면 헤더 영역에 비밀번호 없이 접근 가능한 `🔔 알림 수신 등록` 버튼을 추가했습니다.
   * 이름(부서/직급 포함), 알림 매체(이메일/텔레그램), 수신처 값을 기입하고 저장하면 JSONB 구조(`receivers` 배열)에 자동으로 데이터가 매핑되어 쌓입니다.
2. **관리자 전용 알림 수신처 삭제 및 제어**:
   * 패스코드 `850` 인증을 통과한 뒤 진입하는 설정 모달 내부 하단에 등록된 수신처 매핑 테이블을 렌더링하도록 했습니다.
   * 일반 접속 고객은 이 테이블을 조회만 할 수 있고, `?admin` URL 매개변수로 진입한 관리자(Admin)에게만 `🗑️ 삭제` 버튼이 작동하도록 비즈니스 인가 장치를 통합했습니다.
3. **하위 호환 콤마 변환 동기화**:
   * 웹 대시보드에서 신규 사용자를 등록하거나 기존 수신처를 삭제할 때마다, 구조화된 `receivers` JSON 배열을 자동으로 파싱하여 콤마로 나열된 문자열로 병합 후 `alert_emails` 및 `telegram_chat_ids` 컬럼에 실시간으로 동시에 PATCH 하여 현장 PC와의 하위 호환성을 유지했습니다.

---

## 6. 6차 개발 성과: 업로더 프로그램 UI 현대화 및 지점 한글명 설정 폼 연동 (2026년 7월 7일)

현장 PC용 업로더 GUI의 UI/UX를 프리미엄 다크 네이비 테마로 리뉴얼하고, 관리자가 대시보드 상에서 지�3. **🤖 Vercel 서버리스 & Webhook 기반 텔레그램 봇 자가등록 (/등록) 구현**:
   * 사용자가 텔레그램 봇 대화방에서 `/등록 [디바이스ID] [사용자명] [패스코드]` 명령을 전송하면, 텔레그램 Webhook이 작동하여 **Vercel Serverless Function (`/api/telegram-webhook.js`)**을 호출합니다.
   * Vercel 백엔드 함수가 호출되는 즉시 Supabase `device_config`를 쿼리하여 기기 비밀번호를 보안 검증하고, 일치 시 `receivers` JSON 배열에 사용자 정보를 자동 추가(PATCH) 및 웰컴 메시지를 전송합니다.
   * 기존 로컬 업로더(`gui_uploader.py`)에 임시 기재되어 10초 주기로 부하를 유발하던 getUpdates 폴링 백그라운드 스레드 로직을 **완전히 삭제 및 원복**하여 업로더의 초경량 구조를 유지했습니다.

---

## 10. 최종 품질 및 동작 검증 결과
* **2026-06-18 배포 및 검증 완료**: 로컬 빌드 컴파일 확인 및 SMTP 연동 확인 완료. 깃허브 원격 푸시 후 Vercel 라이브 환경에서 번들 컴포넌트 네임스페이스 충돌 및 컴포넌트 초기화 전 TDZ 참조 오류로 발생했던 화면 멈춤 크래시 현상을 해결하기 위해 핫픽스 배포를 최종 완료하고 정상 작동을 검증했습니다.
* **2026-07-07 3차 개발 및 빌드 검증 완료**: 파이썬 파일 구문 컴파일 검증 성공 및 대시보드 리팩토링 후 Vite 프로덕션 번들 빌드 확인.
* **2026-07-07 4차 V2 설계 및 데이터 연속성 연동 완료**: `site_config_v2` 및 `measure_logs_v2` 기반 웹 대시보드 데이터 조회/수정/삭제 쿼리 정합성 검증 완료.
* **2026-07-07 5차 고객 자가등록 및 관리자 제어 권한 분리 완료**: 자가 등록 완료 시 Supabase `site_config_v2` 내의 JSONB 및 flat columns가 실시간 동기 업데이트됨을 검증했습니다.
* **2026-07-07 6차 업로더 v5.1 UI 현대화 및 지점 한글명 제어 완료**: 현장 PC uploader GUI 화면을 프리미엄 다크 네이비 테마로 리뉴얼하고 접이식 상세 설정 윈도우 및 영구 저장 기능을 확인했습니다.
* **2026-07-07 7차 업로더 설정 간소화 및 세부 주기/기동 제어 완료**: 웹 대시보드에서 지점 한글명을 갱신하면 현장 업로더가 스레드를 통해 감지하는 즉시 UI 라벨과 로컬 config 설정 파일을 동적으로 자동 업데이트하도록 상호 동기화 로직을 검증했습니다.
* **2026-07-07 8차 고지연 현장망 복원 및 타임아웃 완화 검증 완료**: 30초 타임아웃 지연 및 최대 3회 재시도를 적용하여 사내망/VPN 환경에서도 끊김 없는 원격 동기화가 유지되도록 검증 완료했습니다.
* **2026-07-07 9차 Supabase V3 기기 설정 독립 및 대시보드 다중 라우팅 검증**: `device_config` 테이블 생성 후, 웹 대시보드 접속 URL 파라미터에 따라 지점 단위 혹은 기기 단위 쿼리를 독립적으로 수행하여 데이터 흐름과 설정을 완전 분리 격리함을 검증 완료했습니다.
* **2026-07-07 10차 Device ID 수정 제한 및 2단계 경보 검증**: 업로더 GUI 내 Device ID 필드의 수정 제한(readonly)과 대시보드 내 주의(주황색) 및 경고(빨간색) 임계치별 실시간 글자 색상 분기 렌더링, 그리고 업로더 스레드의 caution/warning/alert_level 연계 안정성을 최종 검증 완료했습니다.
* **2026-07-07 11차 미등록 지점/기기 접속 통제 검증 완료**: URL에 등록되지 않은 임의의 ID를 입력할 때 로그인 게이트 우회를 완전 차단하고, 전용 접속 제한 예외 화면을 출력하도록 로직을 반영해 정합성을 검증 완료했습니다.
* **2026-07-07 12차 업로더 auto 기동 시 Fail-safe 안전 차단 검증 완료**: 설정 파일 내 지점 식별자 또는 이름이 auto인 경우 기동 즉시 일시정지 대기 모드가 되며, Supabase로의 신규 기기 등록(POST) 및 비동기 갱신(PATCH) 호출이 전 과정에서 완벽히 통제됨을 모의 테스트 스크립체 실행으로 최종 검증 완료했습니다.
* **2026-07-07 13차 텔레그램 발송 직접화 및 패스코드 변경 검증 완료**: 대시보드 상에서 0초 딜레이로 직접 테스트 텔레그램을 발송하고, 알림 등록/해제 시 실시간 확인 텔레그램 메시지가 사용자에게 도착함을 검증했습니다. 또한 패스코드 및 봇 토큰 동적 수정과 수신처 삭제 권한 완화의 정합성을 검증 완료했습니다.
* **2026-07-08 14차 UI 콤팩트화 및 Vercel 서버리스 Webhook 기반 텔레그램 자가등록(/등록) 검증 완료**: 주의/경고 폰트 스타일 적용 및 설정 모달 확대/세로 스크롤/Grid Layout 채널 카드 리팩토링 검증. 텔레그램 Webhook 발동 시 Vercel 서버리스 API가 가동되어 `/등록 [기기ID] [이름] [패스코드]` 패스코드 검증 후 Supabase receivers JSON 내 자가등록 피드백이 0초 만에 완수됨을 최종 검증 완료.
�� config 설정 파일을 동적으로 자동 업데이트하도록 상호 동기화 로직을 마무리했습니다.

---

## 8. 8차 개발 성과: 산업용 고지연 내부망 대응 및 SSL 핸드셰이크 재시도(Retry) 도입 (2026년 7월 7일)

1. **네트워크 강건성 헬퍼 메소드 (`make_supabase_request`) 신설**:
   * HTTPS 통신 불안정을 해결하기 위해 공통 HTTPS 호출 모듈을 구축하고 최대 3회 재시도(Retry) 메커니즘을 통합했습니다.
2. **타임아웃 임계값 대폭 완화 (30초)**:
   * 패킷 지연 및 프록시 SSL 핸드셰이크 지연을 막고자 타임아웃 임계값을 **30초**로 상향 적용했습니다.
3. **API 커넥션 전면 마이그레이션**:
   * 업로더 내부 모든 REST API 호출 부위를 공통 헬퍼 구조로 전면 교체하여 예외 복원력을 완성했습니다.

---

## 9. 9차 개발 성과: Supabase V3 기기 설정 테이블 신설 및 대시보드/업로더 다중 기기 독립 관리 (2026년 7월 7일)

1. **기기 고유 설정 테이블(`device_config`) 구조 정립**:
   * `device_id`를 PK로 하는 테이블을 신설하여, 다중 기기가 하나의 지점 설정(`site_config_v2`)을 덮어쓰던 문제를 근본적으로 해결했습니다.
2. **웹 대시보드 URL 조건별(?site= 및 ?device=) 라우팅 분기 쿼리 구현**:
   * `device` 파라미터 유입 시 `Device_ID` 컬럼으로, `site` 파라미터 유입 시 `Site_ID` 컬럼으로 데이터 및 설정을 분기 조회하도록 구현 완료했습니다.
3. **업로더 프로그램 동기화 기준 기기 정밀화**:
   * 서버 최신 데이터 시각 조회 필터를 `Device_ID` 기준으로 교체하여 다중 기기 충돌 유실을 방지하고, GUI에서 Site ID and Device ID를 둘 다 명시적으로 입력하고 수동 편집할 수 있도록 UI 그리드를 재구조화했습니다.

---

## 11. 11차 개발 성과: Device ID readonly 및 Caution/Warning 2단계 임계치 설정 고도화 (2026년 7월 7일)

1. **Device ID 수동 오기 방지 (Readonly)**:
   * 계측기 GUI Uploader 내 `Device ID` 입력 필드의 상태를 `readonly`로 제어하여, 사용자가 잘못 입력하는 휴먼 에러를 방지하되 텍스트 선택 및 복사(Ctrl+C)는 유연하게 허용하도록 설계했습니다.
2. **주의(Caution) / 경고(Warning) 2단계 경보 관리**:
   * 대시보드 관리자 UI에 채널별 `caution` 및 `warning` 임계값을 별도 관리하고, 알람 발송을 트리거할 수 있는 하한 레벨 `alert_level` (주의 이상 / 경고 이상)을 통합 지정할 수 있도록 임계치 JSON 형태를 확장 적용했습니다.
3. **대시보드 실시간 글자 색상 분기**:
   * 계측된 데이터 수치가 주의(Caution) 기준 이상일 때는 주황색(`orange`), 경고(Warning) 기준 이상일 때는 빨간색(`var(--accent-rose)`)으로 실시간 글자 색상과 램프 도트의 색상이 자동으로 바뀌도록 분기 렌더링을 적용했습니다.
4. **업로더 알람 처리 스레드 안정성**:
   * 로컬 SMTP 및 텔레그램 경보 검사 루틴이 2단계 설정 형태를 파싱할 수 있게 리팩토링하여 하위 호환성을 완벽하게 보장했습니다.
5. **미등록 지점/기기 무단 접속 전면 차단 (보안 강화)**:
   * 데이터 조회 실패 시 하드코딩된 패스코드 `'850'`으로 우회 접속이 가능했던 보안 결함을 보완하기 위해, 조회 결과가 없을 경우 `isValidDevice: false`로 표시하고 대시보드 화면 대신 접속 제한 보안 차단 경고창을 강제 출력하도록 조치했습니다.

---

## 12. 12차 개발 성과: 업로더 기동 시 auto 상태 자동 업로드/등록 안전 차단 (Fail-safe) 구현 (2026년 7월 7일)
1. **auto 기동 시 일시정지 강제 적용**:
   * 설정 파일의 지점 ID(`site_id`) 또는 지점명(`site_name`)이 `"auto"` 이거나 공백인 채로 프로그램이 켜질 때, `is_paused = True`로 강제 오버라이드하여 데이터 동기화를 차단하고 LED를 주황색(일시정지)으로 띄웁니다.
2. **Supabase 등록 및 갱신 API 호출 차단**:
   * `auto_register_device_config` (신규 기기 등록) 및 `bg_update_site_name_on_supabase` (지점 한글명 PATCH 갱신) API 요청이 나갈 때 지점명이 auto/공백이면 POST/PATCH 전송을 원천 차단하여 DB의 설정 정보 오염을 예방합니다.
3. **백그라운드 스케줄러 일시정지 연동**:
   * 일시정지(Paused) 상태일 때 10초 주기로 돌아가던 텔레그램/이메일 테스트 감지 스레드가 작동하지 않도록 추가 방어하여, 불필요한 백그라운드 Supabase API 쿼리를 완벽하게 통제합니다.

---

## 13. 13차 개발 성과: Vercel 직접 텔레그램 발송 및 접속 패스코드 설정창 제어 구현 (2026년 7월 7일)
1. **Vercel(웹 브라우저) 직접 텔레그램 발송**:
   * 로컬 파이썬 업로더에 의존하던 테스트 발송 및 알림 등록/삭제 피드백 메시지를 웹 브라우저 단에서 텔레그램 Bot API(`api.telegram.org`)를 통해 **즉시(0초 딜레이)** 발송하도록 개선했습니다.
2. **접속 패스코드(비밀번호) 및 자체 봇 토큰 설정창 제어**:
   * 설정 모달 및 관리자 설정 카드 내에 대시보드 접속 패스코드(Passcode)를 직접 변경할 수 있는 입력란을 신설하고 Supabase DB와 연동했습니다.
   * 사용자 정의 텔레그램 봇 토큰(`telegram_bot_token`) 입력창을 추가하여 `toc_alert_high` JSON 내에 패키징 저장하게 했습니다.
3. **수신처 관리 권한 완화**:
   * 패스코드를 입력해 진입한 설정창 내에서는 어드민 URL 여부와 무관하게 **등록된 알림 수신처를 즉시 삭제(`🗑️ 삭제`)**할 수 있도록 통제를 완화했습니다.
4. **파이썬 업로더 봇 토큰 동기화**:
   * 로컬 uploader가 Supabase에서 주기적으로 기기 설정을 읽을 때, 웹에서 지정된 커스텀 `telegram_bot_token`을 자동 감지하여 덮어쓰도록 구현하여 원격 관리를 가능하게 했습니다.

---

## 14. 14차 개발 성과: 설정창 UI 콤팩트화 및 텔레그램 봇 자가등록(/등록) 기능 구현 (2026년 7월 8일)
1. **설정창 UI 대대적 축소 및 사용성 강화**:
   * 대시보드 메인 헤더의 `🔔 알림 수신 등록` 버튼을 완전히 제거하고, 설정 모달 내 `👥 등록된 알림 수신자 목록` 타이틀 옆으로 매립하여 화면을 콤팩트화했습니다.
   * 지점 설정 모달의 너비를 **800px로 확장**하고, 세로 `max-height: 90vh` 및 스크롤을 적용하여 모달이 화면을 침범하지 않도록 사용성을 대대적으로 개선했습니다.
   * 기존에 세로로 길게 늘어지던 임계값 테이블을 **반응형 Grid Card 레이아웃**으로 변경하여 높이를 대폭 줄였습니다.
   * 불필요하게 UI 높이를 차지하던 모달 내 이메일/봇토큰/챗ID 입력창들을 전면 삭제하여, 오직 콤팩트한 지점/패스코드 설정, 채널 임계값, 그리고 등록자 목록만 남겼습니다.
2. **텔레그램 알림 단독 고정**:
   * 일반 등록 양식에서 이메일 등록 옵션을 전면 배제하여 텔레그램 연동 단독 모드로 고정했습니다.
3. **🤖 텔레그램 봇 자가등록 (/등록) 기능 및 보안 패치**:
   * 사용자가 텔레그램 봇 대화방에서 `/등록 [디바이스ID] [사용자명] [패스코드]` 명령을 전송하면, 파이썬 업로더(`gui_uploader.py`)의 백그라운드 스레드가 이를 감지하여 해당 디바이스의 패스코드 일치 여부를 검증합니다.
   * 패스코드 일치 시, Supabase DB의 `toc_alert_high.receivers` 배열에 해당 사용자 Chat ID와 이름을 자동 추가(PATCH)하고 봇을 통해 즉시 웰컴 답장을 쏩니다. (비밀번호 불일치 시 등록 차단 및 경고 전송)
   * 외부 패키지 의존성을 없애기 위해 기본 `urllib` 패키지만 사용하여 연동을 구현했습니다.

---

## 10. 최종 품질 및 동작 검증 결과
* **2026-06-18 배포 및 검증 완료**: 로컬 빌드 컴파일 확인 및 SMTP 연동 확인 완료. 깃허브 원격 푸시 후 Vercel 라이브 환경에서 번들 컴포넌트 네임스페이스 충돌 및 컴포넌트 초기화 전 TDZ 참조 오류로 발생했던 화면 멈춤 크래시 현상을 해결하기 위해 핫픽스 배포를 최종 완료하고 정상 작동을 검증했습니다.
* **2026-07-07 3차 개발 및 빌드 검증 완료**: 파이썬 파일 구문 컴파일 검증 성공 및 대시보드 리팩토링 후 Vite 프로덕션 번들 빌드 확인.
* **2026-07-07 4차 V2 설계 및 데이터 연속성 연동 완료**: `site_config_v2` 및 `measure_logs_v2` 기반 웹 대시보드 데이터 조회/수정/삭제 쿼리 정합성 검증 완료.
* **2026-07-07 5차 고객 자가등록 및 관리자 제어 권한 분리 완료**: 자가 등록 완료 시 Supabase `site_config_v2` 내의 JSONB 및 flat columns가 실시간 동기 업데이트됨을 검증했습니다.
* **2026-07-07 6차 업로더 v5.1 UI 현대화 및 지점 한글명 제어 완료**: 현장 PC uploader GUI 화면을 프리미엄 다크 네이비 테마로 리뉴얼하고 접이식 상세 설정 윈도우 및 영구 저장 기능을 확인했습니다.
* **2026-07-07 7차 업로더 설정 간소화 및 세부 주기/기동 제어 완료**: 웹 대시보드에서 지점 한글명을 갱신하면 현장 업로더가 스레드를 통해 감지하는 즉시 UI 라벨과 로컬 config 설정 파일을 동적으로 자동 업데이트하도록 상호 동기화 로직을 검증했습니다.
* **2026-07-07 8차 고지연 현장망 복원 및 타임아웃 완화 검증 완료**: 30초 타임아웃 지연 및 최대 3회 재시도를 적용하여 사내망/VPN 환경에서도 끊김 없는 원격 동기화가 유지되도록 검증 완료했습니다.
* **2026-07-07 9차 Supabase V3 기기 설정 독립 및 대시보드 다중 라우팅 검증**: `device_config` 테이블 생성 후, 웹 대시보드 접속 URL 파라미터에 따라 지점 단위 혹은 기기 단위 쿼리를 독립적으로 수행하여 데이터 흐름과 설정을 완전 분리 격리함을 검증 완료했습니다.
* **2026-07-07 10차 Device ID 수정 제한 및 2단계 경보 검증**: 업로더 GUI 내 Device ID 필드의 수정 제한(readonly)과 대시보드 내 주의(주황색) 및 경고(빨간색) 임계치별 실시간 글자 색상 분기 렌더링, 그리고 업로더 스레드의 caution/warning/alert_level 연계 안정성을 최종 검증 완료했습니다.
* **2026-07-07 11차 미등록 지점/기기 접속 통제 검증 완료**: URL에 등록되지 않은 임의의 ID를 입력할 때 로그인 게이트 우회를 완전 차단하고, 전용 접속 제한 예외 화면을 출력하도록 로직을 반영해 정합성을 검증 완료했습니다.
* **2026-07-07 12차 업로더 auto 기동 시 Fail-safe 안전 차단 검증 완료**: 설정 파일 내 지점 식별자 또는 이름이 auto인 경우 기동 즉시 일시정지 대기 모드가 되며, Supabase로의 신규 기기 등록(POST) 및 비동기 갱신(PATCH) 호출이 전 과정에서 완벽히 통제됨을 모의 테스트 스크립트 실행으로 최종 검증 완료했습니다.
* **2026-07-07 13차 텔레그램 발송 직접화 및 패스코드 변경 검증 완료**: 대시보드 상에서 0초 딜레이로 직접 테스트 텔레그램을 발송하고, 알림 등록/해제 시 실시간 확인 텔레그램 메시지가 사용자에게 도착함을 검증했습니다. 또한 패스코드 및 봇 토큰 동적 수정과 수신처 삭제 권한 완화의 정합성을 검증 완료했습니다.
* **2026-07-08 14차 UI 콤팩트화 및 텔레그램 봇 자가등록(/등록) 검증 완료**: 주의/경고 폰트 스타일 적용 및 설정 모달 가로폭 800px 확대/세로 스크롤/Grid Layout 채널 카드 리팩토링 검증. 텔레그램 봇에 `/등록 [기기ID] [이름] [패스코드]` 전송 시 패스코드 검증 후 Supabase receivers JSON 내 자가등록 성공 피드백 완료 검증.

