# 에이전트 지침서 (Agent Instructions)

이 프로젝트(TOC850_WebDashboard)에서 작업할 때 에이전트가 준수해야 하는 지침입니다.

## 1. 프로젝트 개요 및 목적
- **프로젝트 명**: TOC850_WebDashboard
- **목적**: TOC(총유기탄소) 측정 계측기 데이터를 Supabase 데이터베이스에 업로드하고, 이를 웹 대시보드로 시각화하여 모니터링 및 알람(이메일 발송 등)을 처리하는 시스템입니다.
- **주요 구성**:
  - `gui_uploader.py`: 계측기 PC에서 실행되며 데이터를 Supabase에 업로드하고 설정된 임계치에 따라 메일을 발송하는 Python GUI 프로그램
  - `dashboard/`: Supabase 데이터를 실시간으로 모니터링하고 알람 임계치를 설정하는 React/Vite 기반 웹 대시보드

## 2. 작업 수행 시 필수 준수 사항
- **회의록 및 개발 이력 관리**:
  - 사용자 요구사항의 변경이나 작업 완료 시, 반드시 [meeting_notes](file:///d:/antigravity/db_upload_and_dashboard/meeting_notes) 폴더 내에 일자별 회의록(예: `2026-06-18_alert_and_test_page_design.md`)을 작성해야 합니다.
  - 프로젝트의 진척 및 작업 현황은 루트의 [2_투두리스트_Task_List.md](file:///d:/antigravity/db_upload_and_dashboard/2_투두리스트_Task_List.md)에, 누적 개발 타임라인 및 기술 내역은 [3_히스토리_및_완료보고서.md](file:///d:/antigravity/db_upload_and_dashboard/3_히스토리_및_완료보고서.md)에 기록하고 최신화해야 합니다.
- **데이터베이스 스키마 변경 제한**:
  - Supabase 데이터베이스의 기존 테이블 구조는 변경하지 않습니다. 임계치 설정 등 추가 데이터는 기존 `toc_alert_high` 컬럼의 JSON 구조 내에 기록하여 확장합니다.
- **Git 푸시 제한**:
  - 로컬 테스트 및 사용자의 최종 확인이 완료되기 전까지는 원격 저장소에 `git push`를 하지 않고 로컬 커밋만 유지합니다.
- **답변 언어**:
  - 사용자와의 소통 및 최종 설명은 항상 명확하고 친절한 한국어(한글)로 작성해야 합니다.

## 3. 프로젝트 인프라 가동 및 샌드박스 배포 상태 (2026-07-07 기준)
* **실서버 대시보드 (기존 고객용 운영 환경)**:
  * **URL**: `https://toc-850-web-dashboard.vercel.app/`
  * **코드 버전**: GitHub `main` 브랜치의 **6월 18일자 최종 안정 커밋 `4ade1f9`** (V2 개별 테이블 기반).
  * **원칙**: 테스트 버전이 완벽히 검증되고 실서비스 적용이 확정될 때까지 **절대 이 빌드를 수정하거나 건드리지 않고 유지**해야 합니다.
* **테스트 대시보드 (개발 및 신기능 검증 환경)**:
  * **URL**: `https://dashboard-ochre-two-35.vercel.app/`
  * **코드 버전**: 로컬 `feature/stage-caution-warning` 브랜치에 저장되어 있으며, Vercel CLI(`npx vercel --prod`)를 통해 수동 강제 배포되어 구동 중인 V3 버전.
  * **원칙**: 다중 기기 독립 설정(`device_config` 기반) 및 2단계 Caution/Warning 임계치 화면 시각화 테스트 공간으로 사용합니다.
* **현장 계측기 PC uploader 배치 현황**:
  * **납품 예정 신규 장비 4대**: 최신 업로더(v5.2 - auto 기동 대기 Fail-safe 탑재 및 2단계 임계값 대응) 기동 완료.
  * **운영 중인 기존 사이트 1곳**: 기존 업로더(V2)와 최신 업로더(v5.2)가 상호 데이터 간섭 없이 동시에 안정 가동 중 (검증 완료).
* **차기 구현 예정 핵심 작업**:
  * 테스트 대시보드(v3) 상의 텔레그램 메시지 발송 연동 튜닝
  * 주의(Caution) 및 경고(Warning) 2단계 기준치 설정 대시보드 마이그레이션 마무리 및 로컬 uploader 전송 정합성 연동 검증.

## 4. 최종 배포 및 산출물 백업 규칙 (수동 NAS 동기화)
- **업로더 빌드 및 릴리즈 배포 시**:
  - 반드시 자동 빌드 도구(`build_release.py`)를 통해 무결한 실행 파일과 가이드를 재생산합니다.
  - 빌드 완료 후 최종 배포 패키지와 파이썬 원본 소스코드를 사내 NAS 경로인 `Z:\05. 개발부\01. 개발프로젝트\01. TOC850\08. 소프트웨어\05. TOC850_웹대시보드\` 하위의 지정 폴더(`01_Uploader_배포패키지`, `02_Uploader_원본코드`)에 덮어쓰기 백업하여야 합니다.
- **웹 대시보드 업데이트 시**:
  - 로컬 `dashboard/` 프로젝트의 최신 소스코드를 `03_대시보드_소스코드/` 경로에 백업하되, 의존성 폴더(`node_modules`, `dist`, `.vercel`)는 반드시 제외하여 백업 부하를 최소화합니다.
  - 인프라 환경변수, Supabase 접속 정보 및 API Keys 등의 인프라 구성 데이터는 `04_운영관리_인프라정보.md`에 최신화하여 관리합니다.
- **메뉴얼 및 설정 가이드 업데이트 시**:
  - 현장 엔지니어가 업로더를 설치하고 DB를 연동하는 퀵 매뉴얼은 `Z:\05. 개발부\01. 개발프로젝트\01. TOC850\06. 카달로그, 사양서, 메뉴얼\05. TOC850_웹대시보드_설정메뉴얼\` 아래에 최신 버전으로 작성해 동기화합니다.

