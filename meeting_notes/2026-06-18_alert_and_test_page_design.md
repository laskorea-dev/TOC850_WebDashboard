# 회의록: 3단계 알림 이메일 연동 및 테스트 페이지 설계안

- **일시**: 2026년 6월 18일 (목)
- **참석자**: USER, Antigravity AI
- **프로젝트**: TOC850_WebDashboard

---

## 1. 완료된 주요 작업 및 연동 현황

### A. 차트 기본 표시 범위 24시간 변경 (요구사항 1)
- 대시보드 메인 화면 로드 시 트렌드 차트의 초기 표시범위를 기존 7일(`7d`)에서 **24시간(`24h`)**으로 갱신 완료했습니다.

### B. 3단계 알림 시각화 및 DB 연동 (요구사항 2)
- 계측 수치 한계를 **평시(흰색) · 주의(노랑) · 경고(빨강)** 3단계로 구분하여 테이블 행 배경색 및 펄스 점(alert-dot)으로 시각화했습니다.
- 채널별 임계치 및 수신자 이메일 목록을 관리자 설정 모달에서 웹으로 제어하고, 기존 DB 스키마 DDL 수정 없이 `toc_alert_high` JSON 필드에 통째로 묶어 Supabase에 저장하도록 통합하였습니다.
- 설정값이 지정되지 않았거나 조회에 실패할 때 사용할 채널별 초기값(폴백) 사양은 다음과 같이 세팅되었습니다:
  - **Ch 3 (방류수)**: 주의 40 / 경고 50 (ppm)
  - **Ch 2 (1차처리수/고농도조)**: 주의 900 / 경고 1000 (ppm)
  - **Ch 1 (유입수/원수조)**: 주의 1600 / 경고 2000 (ppm)
  - **기타 채널**: 주의 5000 / 경고 6000 (ppm)

### C. 경고 감지 시 SMTP 이메일 발송 자동화 (요구사항 2)
- 데이터가 Supabase 클라우드로 성공적으로 INSERT될 때, 신규 데이터 중 경고 한계를 초과하는 건이 있으면 자동으로 SMTP 이메일을 수신자 목록에 발송하는 기능을 파이썬 업로더(`gui_uploader.py`)에 연동하였습니다.
- **메일 발송용 SMTP 서버 설정 (ecounterp)**:
  - SMTP 서버: `wsmtp.ecounterp.com` (Port: 587)
  - 발신 계정 ID: `toc850@las-korea.com` (PW: `toc8503310!`)
- **이메일 폭주 방지(쿨다운)**: 각 채널별로 경고 메일 발송 성공 시 **1시간(3600초) 쿨다운 타이머**가 작동하여 이메일 과다 수신을 방지합니다.

---

## 2. 관리자 테스트용 독립 웹페이지(?admin) 설계 검토 및 합의

추후 내부적으로 데이터 시뮬레이션 및 알림 발송 검증을 안전하게 테스트하기 위해 `?admin` 주소 기반의 독립 웹페이지를 구현하는 아키텍처에 대해 논의하였으며, 아래와 같은 보완 대책을 반영하여 개발을 연동하기로 합의했습니다.

### A. 진입 보안 통제 (보안성 확보)
- 단순히 URL 주소창에 `?admin`만 쳐서 들어오는 위장 침입을 방지하기 위해, 테스트 페이지 진입 시에도 **기존 보안 패스코드(Passcode: 850)**를 요구하는 잠금 해제 게이트를 배치하여 관리자 권한을 이중 검증합니다.

### B. 테스트 데이터의 일반 대시보드 노출 차단 (데이터 격리)
- 테스트용으로 수동 적재한 모의 데이터(`Add_note` 항목이 `[MOCK TEST DATA]`인 행)는 일반 사용자가 보는 메인 모니터링 대시보드 화면 및 차트 조회 결과에서는 **필터링(Exclude)하여 아예 렌더링하지 않도록** 쿼리를 통제합니다. 
- 이로써 내부 개발진이 자유롭게 경보 테스트를 수행하는 중에도 고객이 보는 화면은 정상 데이터만 완벽하게 보장됩니다.

### C. 안전한 원격 이메일 테스트 발송 프로세스
- 브라우저에 SMTP 비밀번호를 하드코딩하지 않고 보호하기 위해 웹에서 테스트 메일 버튼을 클릭하면 Supabase 설정 필드에 임시 플래그(`trigger_test_email: true`)를 저장합니다.
- 계측기 PC의 파이썬 업로더가 백그라운드에서 **10초 주기로 이 플래그를 체크**하여, 참(True)일 때 업로더 PC 내의 안전한 자격 증명으로 테스트 메일을 쏘고 플래그를 다시 거짓(False)으로 내리는 안전한 비동기 발송 로직을 적용합니다.

---

## 3. 향후 적용 로드맵 및 조치 완료 이력

1. **임계치 유효성 검사 완화 (추가 조치)**:
   - 어드민 설정 저장 시 5000 미만 값 입력에 대한 경고 확인창(confirm)을 제거하여, 50 ppm(방류수), 1000 ppm(고농도조) 등 현장 최적의 경고 기준치를 아무런 팝업 방해 없이 원클릭으로 즉시 저장할 수 있도록 유효성 검증 사양을 개선 완료하였습니다.
2. **채널 렌더링 정렬 규칙 최적화 (추가 조치)**:
   - 데이터 노출 출현 순서대로 무작위 배치되던 채널 리스트를 채널 고유 번호 기준 오름차순(`Ch 1 → Ch 2 → Ch 3 ...`)으로 정렬되도록 리액트 상태 렌더링 코드를 개선하여 UI의 직관성을 보강하였습니다.
3. **원격 저장소 실장 배포 완료**:
   - 로컬 구동 테스트 및 사용자의 최종 최종 검증 사양 조율을 거쳐, 금일 개발된 모든 변경사항(알람 추상화 모듈, `?admin` 페이지, 2단계 경고 단순화 등)을 깃허브 원격 저장소(`git push origin main`)에 성공적으로 배포 완료하였습니다.
