# 📋 TOC B2B 멀티테넌시 계측 모니터링 시스템 구축 투두리스트 (Task List)

본 문서는 프로젝트 진행에 따른 세부 구현 요구사항의 구현 이력과 완료 검증 상태를 나타내는 작업 진척 보고서입니다.

---

## 1. 종합 진척 상황 (Overall Progress)

* **종합 진척률**: **100% (완료)**
* **목표 항목**: SQLite Uploader GUI 개발, B2B 멀티테넌시 기기 식별자(`Device_ID`) 데이터 매립 가공, Vite React 다중 기기 실시간 필터 및 차팅 렌더링, E2E 다중 기기 교대 모의 전송 시뮬레이션 최종 검증 완료.

---

## 2. 세부 개발 Task 현황

### Phase 1: 로컬 Python Uploader 코어 엔진 개발 (완료 100%)
* [x] **구글 API 인증 키(`credentials.json`) 연동 구조 설계**
* [x] **SQLite DB 뷰 쿼리 및 증분 추출 로직 구현**
* [x] **gspread 라이브러리 연동 및 일괄 추가(`append_rows`) 최적화**
* [x] **네트워크 예외 처리 및 `uploader.log` 디버깅 로그 누적 장착**

### Phase 2: React 웹 대시보드 개발 환경 및 스타일링 (완료 100%)
* [x] **Vite + React 신규 프레임워크 템플릿 초기화**
* [x] **Recharts 차트 시각화 패키지 및 NPM 디펜던시 셋업**
* [x] **디자인 시스템 및 CSS 글로벌 다크모드 코딩 (`src/index.css`)**

### Phase 3: 대시보드 핵심 시각화 및 데이터 바인딩 (완료 100%)
* [x] **구글 시트 웹 게시 연동 및 로컬 CSV Fetcher API 이중 연동**
* [x] **4대 KPI 요약 통계 카드 개발**
* [x] **TOC 시계열 변동 추이 차트 및 채널별 필터 버튼그룹 구현**
* [x] **채널 간 최고/평균 농도 비교 바 차트 구현**
* [x] **이력 상세 조회 테이블 및 엑셀 CSV 내보내기(다운로드) 탑재**
* [x] **가상 데이터 주입 시뮬레이터 UI 탑재**

### Phase 4: 데스크톱 GUI 업로더 프로그램 개발 (완료 100%)
* [x] **Tkinter 기반 다크 UI 레이아웃 설계**
* [x] **연동 SQLite DB [찾아보기] 파일 탐색 다이얼로그 이식**
* [x] **1초 단위 실시간 자동 업로드 카운트다운 타이머 구현**
* [x] **시작 / 일시정지(`is_paused` 플래그) 및 즉시 업로드 제어 장치**
* [x] **백그라운드 비차단 멀티스레딩(Worker Thread) 연동**
* [x] **로컬 상태 저장 시스템 (`uploader_config.json`) 구현**

### Phase 5: [B2B 고도화] 다중 계측기 식별 및 고객 격리 구현 (완료 100%)
* [x] **업로더 환경 설정 파일 (`uploader_config.json`) 독립 분리 및 중앙 제어 장착**
  * *결과*: 소스 코드를 건드리지 않고 DB 경로, 시트 이름, 가동 주기, 기기 ID를 파일로 쉽게 조율 보관 완료.
* [x] **업로더 GUI 내에 `계측기 장비 고유 ID (Device ID)` 텍스트 창 및 저장 버튼 장착**
  * *결과*: 마우스와 키보드로 프로그램상에서 장비 ID를 즉각 업데이트하고 설정 파일에 영구 기록 연동 완료.
* [x] **데이터 전송 가공 시 `Device_ID` 컬럼 자동 매립 모듈 빌드**
  * *결과*: 헤더 컬럼 구조의 두 번째 위치에 `Device_ID`를 강제 매립하여 전송하도록 포맷팅 모듈 완성.
* [x] **React 대시보드 상단에 B2B `계측기 선택 필터 (Device Select Dropdown)` 드롭다운 장착**
  * *결과*: CSV 데이터로부터 기기 ID를 중복 제거하여 동적으로 셀렉트박스를 채우고 선택에 따라 KPI, 추이 라인 차트, 바 차트, 테이블까지 실시간 연동 연쇄 렌더링 완료.
* [x] **다중 기기 교대 모의 데이터 주입 시뮬레이터 고도화**
  * *결과*: 시뮬레이터 클릭 시 무작위로 `DEVICE_01` 또는 `DEVICE_02`를 교대로 주입하여, 여러 현장의 계측기가 단일 시트에 믹싱 누적되고 필터링되는 B2B 연동 연쇄 렌더링 과정을 로컬에서 완벽 시연 검증 완료.

---

## 3. 최종 품질 및 동작 검증 결과
1. **B2B 데이터 무결성 검증**: SQLite 원천 데이터 가공 시 헤더 구조의 두 번째 인덱스에 기기 ID를 한 줄 누락 없이 포지셔닝하여 동기화하고, 대시보드에서 각 기기별로 완벽 격리 필터링 조회됨을 최종 승인하였습니다.
2. **웹 컴파일 상태**: 다중 기기 필터가 추가된 `App.jsx`를 대상으로 `npm run build` 결과 에러 및 경고 없이 2.13초 만에 빌드 완결되었습니다.
