# 📋 TOC B2B 멀티테넌시 계측 모니터링 시스템 구축 투두리스트 (Task List)

본 문서는 프로젝트 진행에 따른 세부 구현 요구사항의 구현 이력과 완료 검증 상태를 나타내는 작업 진척 보고서입니다.

---

## 1. 종합 진척 상황 (Overall Progress)

* **종합 진척률**: **100%**
* **현재 목표**: 계측기 Uploader의 Device ID 필드 읽기전용화(복사는 허용) 및 주의(Caution)/경고(Warning) 2단계 채널별 임계값 설정 대시보드 연동, 대시보드 텍스트 색상 분기, 로컬 Uploader 알림 트리거 호환 처리를 포함한 Phase 17 최종 개발 및 로컬 최종 검증 완료.

---

## 2. 세부 개발 Task 현황

### Phase 1~5: 로컬 Python Uploader 및 Google Sheets 프로토타입 개발 (완료 100%)
* [x] 구글 Sheets 연동 데이터 전송 및 GUI 업로더 인터페이스 개발 완료 (1차 프로토타입)
* [x] 다중 기기 식별 및 고객사별 대시보드 물리 격리 시뮬레이션 검증 완료

### Phase 6: Supabase 데이터베이스 마이그레이션 및 차트 범위 수정 (완료 100%)
* [x] **Supabase DB 연결 자격 증명 연동 및 데이터 테이블 적재 마이그레이션**
* [x] **대시보드 트렌드 차트 표시범위 기본값을 기존 7일에서 24시간(24h)으로 최적화**

### Phase 7: 3단계 알림 고도화 및 SMTP 이메일 발송 연동 (완료 100%)
* [x] **주의(노랑) / 경고(빨강) 3단계 등급 테이블 시각화 구현**
* [x] **별도 스키마 변경 없이 `toc_alert_high` JSON 필드 내 설정값 일괄 패키징 및 웹 저장 연동**
* [x] **`wsmtp.ecounterp.com` SMTP 서버 연동 및 채널별 1시간 쿨다운 경고 메일 발송 로직 구현**
* [x] **알람 전송 인터페이스 추상화 (`alerts/` 패키지화)**
* [x] **카카오톡/MMS 대비 이메일 발송 모듈화 (`EmailAlertSender` 도입 및 설정 연동)**
* [x] **`?admin` 경로 분기 및 접속용 패스코드(Passcode: `850`) 보안 검증 게이트 구현**
* [x] **테스트용 수동 모의 데이터(`[MOCK TEST DATA]`)의 일반 사용자 대시보드 노출 차단 필터 구현**
* [x] **웹 UI 테스트 메일 발송 비동기 플래그(`trigger_test_email`) 핸들러 및 파이썬 폴링 리셋 로직 연동**
* [x] **설정 저장 시 5000 미만 임계치 설정에 대한 경고 확인창(confirm)을 완전히 제거하여, 모든 범위(0 이상)의 커스텀 경고 기준값을 알림 없이 즉시 저장할 수 있도록 개선**

### Phase 8: 단일 테이블 통합 및 텔레그램 무료 알림 연동 (완료 100%)
* [x] **모든 장비가 단일 테이블(measure_logs)을 공유하여 데이터베이스 자원을 효율화하는 싱글 테이블 아키텍처 구현**
* [x] **API 쿼리 및 증분점 탐색 시 `Device_ID` 필터링 주입 적용 완료**
* [x] **텔레그램 API 연동 발송 모듈(`TelegramAlertSender`) 신규 구축 완료**
* [x] **기존 스키마 DDL 수정 없이 `toc_alert_high` JSON 필드 내에 Telegram Chat ID 및 테스트 트리거(`trigger_test_telegram`) 패키징 저장**
* [x] **웹 대시보드(App.jsx / LegacyApp.jsx)에 Chat ID 입력창 및 즉시 테스트 메시지 발송 신호 연동 완료**

### Phase 9: V2 스키마 설계 및 지점 식별자 직접 매립 아키텍처 (완료 100%)
* [x] **Supabase 신규 테이블 `site_config_v2` 및 `measure_logs_v2` 구조 정의 및 SQL DDL 배포 가이드라인 수록**
* [x] **업로더 프로그램(gui_uploader.py) 내 `Site_ID` (지점 코드) 및 `Device_ID` (장치명) 개별 로드 및 API 연동**
* [x] **서버 증분 탐색(`query_server_latest_datetime`) 시 `Site_ID` 필터 기준으로 조회하여 데이터 정합성 보장**
* [x] **웹 대시보드(App.jsx / LegacyApp.jsx)의 데이터 조회(`loadData`), CSV 다운로드, 모의 데이터 쿼리 전체를 `Site_ID` 기준으로 필터 조회하도록 V2 마이그레이션**
* [x] **V5.0 GUI 업로더 컴파일 (`gui_uploader_v5.0.exe`) 및 배치파일 연동 완료**

### Phase 10: 고객 알람 자가 등록 및 관리자 삭제 권한 분리 (완료 100%)
* [x] **고객이 직접 이메일/텔레그램 알림 수신처(이름, 구분, 수신 주소)를 등록할 수 있는 대시보드 공개 양식 구현**
* [x] **관리자 설정 모달(패스코드 gated)에 등록된 수신자 정보(이름, 형태, 주소) 매핑 테이블 렌더링**
* [x] **관리자 모드(`?admin` 파라미터 활성화)에서만 `🗑️ 삭제` 버튼이 작동하도록 권한 통제 적용**
* [x] **자가 등록 및 삭제 동작 시 `receivers` JSON 배열 변경이 실시간으로 `alert_emails` 및 `telegram_chat_ids` 콤마 분리형 DB flat 필드에 자동 결합 동기화되도록 처리하여 현장 PC 업로더 프로그램 호환 유지**

### Phase 11: 업로더 프로그램 UI 현대화 및 지점 한글명 설정 폼 연동 (완료 100%)
* [x] **업로더 GUI 프로그램에 Segoe UI/맑은 고딕 글꼴 및 프리미엄 다크 네이비/시안 테마(패밀리룩) 적용**
* [x] **원형 캔버스 LED 활성 인디케이터(ACTIVE/PAUSED 글로우 컬러 분기) 적용**
* [x] **상세 설정 숨기기/펼치기 폴더블(collapsible) 레이아웃 및 원클릭 모든 설정 영구 저장 파일 write 연동**
* [x] **웹 대시보드 설정 모달 내 "🏢 지점 한글명 설정" 텍스트 입력창 신설 및 DB PATCH 동기화 연동**
* [x] **배포 패키지 uploader_config.json 내 Supabase URL, Key, default Telegram token 기본 기입 및 v5.1 리비전 패키징 완료**

---

### Phase 12: 업로더 설정 단순화 및 동기화 주기/기동 상태 설정 구현 (완료 100%)
* [x] **불필요한 지점 식별자 코드(Site ID) 입력 필드를 UI에서 제거하여 환경 단순화** (설정 파일에서는 계속 로드/저장 유지)
* [x] **'사이트 이름 (지점 한글명)' 라벨을 직관적인 '사이트 이름 (Site Name)'으로 변경**
* [x] **'자동 동기화 주기 (분 단위)' 입력 필드를 신설하여 분 단위 세밀한 주기 설정 및 초 단위 변환 저장 구현**
* [x] **'시작 시 자동 동기화 활성화' 체크박스를 통해 기동 시 일시정지(`is_paused`) 또는 즉시 구동 상태 지정 및 저장 연동**
* [x] **실시간 큐 이벤트 및 새로고침 주기에서 사이트 이름(`site_name`) 값이 GUI와 상호 즉시 동기화되도록 수정**
* [x] **배포 패키지 디렉토리(`계측기_PC_배포패키지_v5.1`) 내 단일 실행 파일 및 uploader_config.json 설정 구성 완료**

---

### Phase 13: 현장 네트워크 불안정 대응 및 커넥션 핸드셰이크 예외 완화 (완료 100%)
* [x] **공통 HTTPS API 호출용 `make_supabase_request` 헬퍼 메소드 구현**
* [x] **일시적 망 유실/지연 극복을 위한 최대 3회 자동 재시도(Retry) 메커니즘 통합**
* [x] **SSL 핸드셰이크 타임아웃 방지를 위해 기존 5~8초로 한정되어 있던 API 타임아웃 제한을 30초로 완화**
* [x] **`gui_uploader.py` 내 모든 GET/POST/PATCH Supabase 연결 부위를 신규 헬퍼로 전면 마이그레이션 및 바이너리 재빌드 완료**
* [x] **Supabase `measure_logs_v2` 테이블 스키마에 정의되지 않은 `MAXR` 컬럼을 JSON 전송 페이로드에서 사전 제외하여 HTTP 400 Bad Request 에러 완벽 해결**
* [x] **Tkinter UI 그리드 패킹 순서를 최적화하여 하단 `[즉시 동기화]` 및 `[일시정지]` 컨트롤 버튼 프레임이 항상 노출되도록 보장**

---

## 3. 최종 품질 및 동작 검증 결과
1. **차트 기본 범위 검증**: React 앱 로드 시 `App.jsx`에서 `24h`가 기본 활성화 상태로 로드되어, 대량 데이터 조회 시의 초기 로딩 부하를 크게 감소시켰습니다.
2. **이메일 발송 상태**: python uploader에서 설정된 임계치 초과 시 ecounterp SMTP를 통해 경고 메일이 발송되는 코어 모듈 동작을 확인했습니다.
3. **단일 테이블 및 텔레그램 푸시 알림 검증**:
   * `VITE_USE_SINGLE_TABLE` 환경변수가 활성화되면 대시보드에서 단일 테이블(`measure_logs` 등)에 `Device_ID` 필터를 적용하여 데이터를 로드합니다.
   * `TelegramAlertSender` 모듈이 오류 없이 컴파일되었으며, 10초 주기의 백그라운드 스레드 폴링을 통해 웹에서 요청한 `trigger_test_telegram` 신호를 성공적으로 캐치하여 초기화합니다.
4. **V2 스키마 및 Site_ID 연속성 연동 검증**:
   * `site_config_v2` 및 `measure_logs_v2` 통합 데이터 테이블 조회를 구현하여, PC 교체 시에도 과거 데이터가 한 화면에 매끄럽게 연결되는 아키텍처 구축을 검증 완료했습니다.
5. **고객 자가등록 및 관리자 제어 검증**:
   * 대시보드 메인 화면의 `🔔 알림 수신 등록` 버튼을 클릭하여 누구나 이름과 수신처를 입력해 자가 신청할 수 있습니다.
   * 이 정보는 `receivers` JSONB 필드로 구조화 관리되며, `?admin` 모드로 진입한 관리자만 삭제 버튼을 보고 제어할 수 있도록 안전하게 인가 격리되었습니다.
   * 저장/삭제 시 SQLite 기반 현장 PC uploader가 참조하는 flat 컬럼 데이터(`alert_emails`, `telegram_chat_ids`)가 콤마로 즉시 변환 동기화되어 이전의 하위 호환성을 그대로 보존합니다.
6. **업로더 v5.1 UI 현대화 및 지점 한글명 관리 검증**:
   * 현장 PC uploader GUI 화면을 프리미엄 다크 네이비 테마로 리뉴얼하고, 접이식 상세 설정 윈도우 및 영구 저장 기능을 완성했습니다.
   * 대시보드 어드민 설정 폼에서 지점 한글명을 기입하고 저장 시, Supabase의 `site_name`이 즉시 갱신되어 상단 헤더에 동적 연동 표시됨을 확인했습니다.
   * 배포 패키지 v5.1 내에 SUPABASE URL/Key 및 텔레그램 봇 토큰 값을 매립 동봉하여 현장 납품 및 구동 준비 상태를 완료했습니다.
7. **업로더 설정 최적화 및 간소화 검증**:
   * 불필요했던 Site ID 입력 필드를 화면에서 완전히 숨겨 사용자 실수를 방지하고, 사이트 이름 표기를 직관적으로 일원화했습니다.
   * 동기화 주기(분 단위)와 기동 시 자동 동기화 체크박스를 UI 전면 배치하여, 사용자가 현장 네트워크 환경에 맞게 간편하게 주기를 튜닝하고 일시정지 상태 기동 여부를 클릭 한 번으로 지정할 수 있음을 검증했습니다.
8. **네트워크 장애 감쇄 및 핸드셰이크 복원력 검증**:
   * 판넬 PC 등 산업용 고지연 내부망에서 흔히 일어나는 SSL 핸드셰이크 타임아웃 문제를 해결하기 위해 타임아웃을 30초로 설정하고, 1.5초 간격으로 최대 3회까지 재시도하는 로직의 복원력을 실시간 로그 기록을 통해 완벽히 가동 중임을 확인했습니다.
9. **Supabase DDL 불일치 제거 (HTTP 400 해결)**:
   * 로컬 SQLite 뷰에는 존재하지만 Supabase `measure_logs_v2` DDL에는 선언되지 않은 `MAXR` 항목을 JSON 데이터 페이로드에서 분리 필터링하여 `400 Bad Request` 에러를 최종 퇴치했습니다.
10. **컨트롤 패널 레이아웃 클리핑 해결**:
    * 텍스트 로그창(`logger_frame`)이 스크린 높이를 넘겨 하단 버튼을 덮는 스크롤바 클리핑을 막기 위해, Bottom Control Frame을 `side=tk.BOTTOM`으로 사전 할당 배치하여 상시 조작 가능 상태를 확인했습니다.
11. **다중 기기 설정 격리 및 덮어쓰기 방지 검증**:
    * 배포 패키지 내 설정 파일의 기기 ID를 `auto`로 리셋하여, 최초 구동하는 모든 PC가 고유의 물리 호스트네임을 기반으로 원격 설정 행을 분리 생성하도록 구현했습니다.
    * 업로더 GUI 화면에 **지점/기기 식별자 ID (Site ID - 고유값)** 입력 필드를 노출하고 활성화하여, 사용자가 각 계측기 PC별로 자유롭게 고유한 키(예: `TOC-260706-03` 등)를 할당 및 갱신해 개별 지점명, 패스코드, 알림 대상자 풀을 완전히 격리 관리할 수 있음을 검증했습니다.

---

### Phase 14: 다중 기기 설정 덮어쓰기 방지 및 지점 식별자 직접 편집 지원 (완료 100%)
* [x] **배포용 템플릿 uploader_config.json 내 site_id 및 site_name 기본값을 auto로 전환하여 다중 기기 자동 식별 보장**
* [x] **업로더 GUI 내에 지점/기기 식별자 ID (Site ID - 고유값) 입력 및 변경 제어창 신설 (state="normal")**
* [x] **설정 저장 시 site_id 값 유효성 검사 적용 및 Supabase site_config_v2의 해당 식별자 갱신 로직 연동**
* [x] **바이너리 재빌드 (gui_uploader_v5.1.exe) 및 배포 디렉토리 업데이트 완료**

---

### Phase 15: Supabase V3 기기 설정 테이블 신설 및 대시보드/업로더 다중 기기 독립 관리 (완료 100%)
* [x] **기기 고유 ID (device_id)를 기본키로 사용하는 device_config 설정 테이블 신설 스키마 DDL 작성**
* [x] **웹 대시보드에서 URL 파라미터(?site= 및 ?device=)에 따라 데이터 조회(Device_ID 혹은 Site_ID 기준) 및 설정 동기화 분기 쿼리 구현**
* [x] **업로더 프로그램의 REST API 조회 및 갱신 대상을 신설 device_config 테이블로 전면 변경하고 device_id 고유키 기준으로 동작 구현**
* [x] **업로더 증분점 데이터 최종 시각 쿼리를 Site_ID가 아닌 Device_ID 기준으로 정밀화하여 다중 기기 충돌 유실 방지**
* [x] **업로더 GUI 내에 Site ID와 Device ID를 둘 다 명시적으로 입력하고 수동 편집할 수 있도록 UI 그리드 재구조화**
* [x] **최종 소스 반영 바이너리 (gui_uploader_v5.1.exe) 재빌드 및 배포 패키지 업데이트 완료**

---

### Phase 17: Device ID readonly 및 Caution/Warning 2단계 임계치 설정 고도화 (완료 100%)
* [x] **계측기 GUI Uploader 내 Device ID 입력 필드를 `state="readonly"`로 변경하여 오기 방지 및 복사(Ctrl+C) 가능 조치**
* [x] **대시보드 관리자 설정 화면 및 모달창에 주의(Caution) 및 경고(Warning) 2단계 기준 입력 필드 및 알람 발송 레벨 셀렉트 박스 신설**
* [x] **웹 대시보드 실시간 데이터 테이블 및 어드민 내역의 TOC 수치 글자 색상 분기 렌더링 (주의: 주황색, 경고: 빨간색)**
* [x] **SQLite/Supabase uploader 연동 알람 체크 스레드 내 주의/경고 트리거 기준 및 alert_level 매핑 분기 호환 패치 완료**
* [x] **신규 바이너리 컴파일 (`gui_uploader_v5.2.exe`) 및 배포 패키지(`계측기_PC_배포패키지_v5.2`) 제작 완료**
* [x] **대시보드 접속 주소 파라미터(?site= 또는 ?device=) 입력값 유효성 검사 및 미등록 기기 접속 전면 차단(보안 강화)**
